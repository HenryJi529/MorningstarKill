# 协作规则

- **未授权不允许改文件**：任何文件的创建、修改、删除，必须先获得用户明确授权，不得擅自执行。
- **未授权不允许 git 写操作**：不得擅自执行 `git add`/`commit`/`push` 等写操作；只读操作（status/log/diff）默认可用，写操作须逐次明确授权，且只给出命令文本、不代为执行。
- **文档权威层级**：`docs/` 是面向人的开发史与汇报材料（含已冻结的决策记录），不作为现行行为依据；`openspec/specs/` 是现行规格，`openspec/changes/` 是变更流程入口。任何来源冲突时，以代码和 specs 为准。