<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.morningstar.kill.mapper.UserMapper">
    <select id="selectByUsername" resultType="User">
        select replace(BIN_TO_UUID(id), '-', '') as id, username, password, phone, email, nickname, sex, create_time, update_time
        from user where username = #{username}
    </select>
</mapper>
